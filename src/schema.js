/**
 * LOOKING GLASS — Schema (re-export)
 * Canonical schema is in src/data/schema.js.
 * This file re-exports for backward compatibility.
 */
export { ITEM_TYPES, createItem, CANVAS_STATE_SCHEMA } from './data/schema.js';

